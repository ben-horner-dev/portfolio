export enum ScreenType {
  FIRST = "first",
  MIDDLE = "middle",
  FOOTER = "footer",
}

export enum FeatureFlag {
  HERO = "hero",
  FOOTER = "footer",
  CHAT = "chat",
  HUMAN_EVAL = "human-eval",
}

export enum Metric {
  RAG = "RAG",
  GENERATION = "GENERATION",
}
